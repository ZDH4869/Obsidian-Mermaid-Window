# 一键安装提示词（投喂给 AI Agent）

把下面整段内容投喂给任意能操作本地文件的 AI Agent（如 opencode、Claude Code、Cursor、Trae 等），并让它以本 zip 解压目录为基准，即可自动完成「Mermaid Window」插件的安装与配置。

---

你是 Obsidian 插件安装助手。请严格按以下步骤，把「Mermaid Window」插件安装到用户的 Obsidian 库并完成配置。

## 背景

本目录下有一个插件文件夹 `mermaid-window/`，内含：

- `manifest.json` 插件清单
- `main.js`      插件主逻辑
- `styles.css`   插件样式

插件作用：为 Markdown 中的 Mermaid 流程图提供缩放/平移，并可一键在独立窗口打开（可拖到其他显示器）。

## 安装步骤

1. 确认用户的 Obsidian 库（vault）根目录（即包含 `.obsidian` 文件夹的目录）。
   - 用户未说明时，先询问：「你的 Obsidian 库根目录在哪里？」并等待回答。
2. 在 `<vault>/.obsidian/plugins/` 下创建文件夹 `mermaid-window`。
3. 将 `manifest.json`、`main.js`、`styles.css` 三个文件**原样复制**到该文件夹（不要修改内容）。
4. 检查冲突：若存在 `<vault>/.obsidian/plugins/mermaid-flow-zoom/`，提示用户停用或删除它，避免同一张图被重复增强。
5. 完成后告知用户：重启 Obsidian → 设置 → 第三方插件 → 启用「Mermaid Window」。

## 验收标准

- 打开含 ` ```mermaid ` 代码块的笔记，悬浮流程图出现工具栏。
- 点工具栏「⤢」弹出独立窗口，背景为白色（浅色主题下），可拖动到其他显示器。

## 注意事项

- 本插件 `isDesktopOnly=true`，仅桌面端可用。
- 三个源码文件请原样复制，勿改动。
